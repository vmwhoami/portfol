<h1>My recent works</h1>

<% @portfolios.each do |item| %>
<h2><%= item.title %></h2>
<h3><%= item.subtitle %></h3>
<p><%= item.body %></p>
<%= image_tag item.thumb_image %>
 <%= image_tag item.main_image %> 

<% end %>